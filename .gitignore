# Claude Code local settings and internal memory — never commit
.claude/settings.local.json
memory/

# Environment variables — secrets must never be committed
.env
src/backend/.env

# Python
__pycache__/
*.pyc
*.pyo
.venv/
.uv/

# Node
node_modules/
dist/
.pnpm-store/

# Python tooling artifacts
*.egg-info/
.pytest_cache/
.ruff_cache/

# Graph data — JSON graphs are runtime data; only the demo graph is tracked
data/graphs/*.json
!data/graphs/demo.json

# OS
.DS_Store
Thumbs.db
