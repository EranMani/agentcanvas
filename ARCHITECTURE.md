# Architecture & Data Flow

A living document. Updated as each component is built and each decision is made.
Claude owns this file — all agents flag updates, Claude writes them.

> This file is populated in real time as architecture takes shape during development.
> It starts empty and grows with the project. No pre-calculated entries.

---
